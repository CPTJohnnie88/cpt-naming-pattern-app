# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: cpt-naming-pattern-app
metadata:
    template: azd-init@1.5.1
services:
    src:
        project: src
        host: containerapp
        language: dotnet
        docker:
            path: Dockerfile
